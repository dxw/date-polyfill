jQuery(function () {
    'use strict';

    if (typeof Modernizr === 'undefined' || typeof Modernizr.inputypes === 'undefined') {
        console.warn('date-polyfill: Modenizr must be loaded including the inputtypes option')
    }

    // ...
})
